// interface class: all the methods inside an interface is and abstract method which has no body

public interface Animal {
    //interface method which has no body like an abstract method, this method will be implemented in child class, these methods are public method
    
    void bark();
    int a = 5, b = 6;       // these are final variable , can not change them
    
    
    public void organ();
}